{
  "name": "lab_work",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Laurent",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "start:test": "node index.js",
    "test": "npx playwright test",
    "test:students:get": "npx playwright test -g 'GET /students'",
    "test:students:post": "npx playwright test -g 'POST /students'",
    "test:instructors:get": "npx playwright test -g 'GET /instructors'",
    "test:instructors:post": "npx playwright test -g 'POST /instructors'",
    "test:enrollments:get": "npx playwright test -g 'GET /enrollments'",
    "test:enrollments:post": "npx playwright test -g 'POST /enrollments'",
    "test:courses:get": "npx playwright test -g 'GET /courses'",
    "test:courses:post": "npx playwright test -g 'POST /courses'",
    "test:assignments:get": "npx playwright test -g 'GET /assignments'",
    "test:assignments:post": "npx playwright test -g 'POST /assignments'",
    "test:grades:get": "npx playwright test -g 'GET /grades'",
    "test:grades:post": "npx playwright test -g 'POST /grades'"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemon": "^3.1.11"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
